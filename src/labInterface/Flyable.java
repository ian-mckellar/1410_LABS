package labInterface;

/**
 * an interface that is used to implement the ability to fly in other classes
 * 
 * @author ian
 *
 */
public interface Flyable {

	/**
	 * Prints the launch of a Flyable object
	 */
	void launch();

	/**
	 * Prints the land of a Flyable object
	 */
	void land();
}
